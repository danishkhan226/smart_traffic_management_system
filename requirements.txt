# Core Dependencies
opencv-python>=4.8.0
numpy>=1.26.0
flask>=2.3.0
flask-cors>=4.0.0
matplotlib>=3.8.0
requests>=2.31.0
Pillow>=10.0.0

# YOLO Object Detection (GPU-accelerated)
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Road Network & Routing (for Shortest Path feature)
osmnx>=1.6.0
networkx>=3.0
geopandas>=0.14.0

# Optional: GPU acceleration (CUDA toolkit required separately)
# See: https://pytorch.org/get-started/locally/ for GPU-specific installation
